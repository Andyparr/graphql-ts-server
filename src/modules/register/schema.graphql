# import User, Response, Date from '../../shared.graphql'

type Query {
  getAllUsers: [User!]!
}

type Mutation {
  register(
    firstName: String!
    lastName: String!
    email: String!
    password: String!
  ): [Response!]!
  deleteUser(id: String!): Response!
}
